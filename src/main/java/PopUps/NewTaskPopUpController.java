package PopUps;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;

public class NewTaskPopUpController {
    // Where ill define my GUI controls defined in FXML and used by the controller's code
    //@FXML
    //private type name;

    //discard all changes
    @FXML
    private void cancel(ActionEvent event) {

    }

    //create a new task for the current list with the information entered
    //throw an error message if anything is invalid (name too short, ig date and desc can be blank?)
    @FXML
    private void newTask(ActionEvent event) {

    }
}
