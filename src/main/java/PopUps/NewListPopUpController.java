package PopUps;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;

public class NewListPopUpController {
    // Where ill define my GUI controls defined in FXML and used by the controller's code
    //@FXML
    //private type name;

    //discard all changes
    @FXML
    private void cancel(ActionEvent event) {

    }

    //create a new list using the String entered
    //throw an error message if too short
    @FXML
    private void newList(ActionEvent event) {

    }
}
